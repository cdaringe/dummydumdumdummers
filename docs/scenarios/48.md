# Scenario 48 — Succinct external program calls

- Added `command_runner.sh/1` and `sh_in_dir/2` to wrap `sh -c` for
  single-string commands, removing program+args boilerplate while keeping typed
  StepResult handling. (src/thingfactory/command_runner.gleam)
- Helpers reuse common error/exit handling via new `build_step/2`, so shell
  invocations share the same failure messaging and trimming as existing runners.
- Tests cover both helpers to show they succeed in pipelines
  (`sh_wrapper_success_test`, `sh_in_dir_success_test`).
  (test/command_runner_test.gleam)
