# Scenario 38: CLI Defaults To Docker Isolation

> The CLI SHALL by default run pipelines in isolation. The isolation mechanism
> should be pluggable, but default to docker containers.

## Completed

`thingfactory run` now defaults to Docker isolation and supports explicit
backend selection:

- `--isolator docker|local` (pluggable selector)
- `--docker-image <image>` (Docker backend config)

If `--isolator` is omitted, `docker` is used.

## How It Works

- `run` command arguments are parsed with isolator settings.
- In Docker mode, CLI re-executes itself inside a container and forces the inner
  command to `--isolator local` to prevent recursion.
- The container runs from the current workspace mount (`-v <cwd>:/workspace`)
  and executes:
  `gleam build --target erlang --warnings-as-errors && gleam run -m thingfactory/cli -- ...`

## Evidence

- [`src/thingfactory/cli.gleam`](/Users/cdaringe/src/thingfactory/src/thingfactory/cli.gleam):
  `IsolationMode`, `--isolator`, `--docker-image`, Docker-by-default
  `resolve_isolation_mode`, and Docker execution path (`execute_run_in_docker`).
- [`src/thingfactory/thingfactory_erlang_cli.erl`](/Users/cdaringe/src/thingfactory/src/thingfactory/thingfactory_erlang_cli.erl):
  added `get_cwd/0` FFI for workspace mount path resolution.
- [`src/thingfactory/cli_ffi.mjs`](/Users/cdaringe/src/thingfactory/src/thingfactory/cli_ffi.mjs):
  added `get_cwd()` parity for JS target.
- [`test/cli_test.gleam`](/Users/cdaringe/src/thingfactory/test/cli_test.gleam):
  parser coverage for new isolation flags and updated `Run` command shape.

## Validation

- `gleam test` passes (243 passed, 0 failed).
