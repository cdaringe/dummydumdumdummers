# Scenario 0: Passing Tests and Working Examples

> The project SHALL always have passing tests & working examples.

## Completed

- Fixed default Docker isolation image from non-existent `ghcr.io/gleam-lang/gleam:latest` to valid `ghcr.io/gleam-lang/gleam:v1.13.0-erlang` (`src/thingfactory/cli.gleam:774`).
- Eliminated all 54 compiler warnings across 12 source/test files (unused args, imports, constructors) so `gleam build --warnings-as-errors` succeeds (requirement 12).

## Validation

- `gleam build --warnings-as-errors` — compiles with zero warnings.
- `gleam test` — 247 passed, 0 failed.
- `gleam run -m thingfactory/cli -- list` — prints usage banner.
- `gleam run -m thingfactory/cli -- run thingfactory@examples:basic_pipeline --isolator local` — SUCCESS, 3 steps.
- `gleam run -m thingfactory/cli -- run -f src/thingfactory/examples.gleam parallel_build_pipeline --isolator local -c` — SUCCESS, 5 steps.

## Key Changes

| File | Change |
|---|---|
| `src/thingfactory/cli.gleam` | Default docker image → `ghcr.io/gleam-lang/gleam:v1.13.0-erlang` |
| `src/thingfactory/parallel_executor.gleam` | Removed unused `Pending`/`Running` constructors, unused `traces` param |
| `src/thingfactory/scheduler.gleam` | Prefixed unused `timestamp_ms` args, removed unused imports |
| `src/thingfactory/examples.gleam` | Prefixed unused step args |
| `src/thingfactory/interactive_cli.gleam` | Prefixed unused `state` arg |
| `src/thingfactory/test_helpers.gleam` | Removed unused `type Step` import |
| `src/thingfactory/registry.gleam` | Removed unused `PipelineId` constructor import |
| `test/*.gleam` (6 files) | Prefixed unused args, removed unused imports |
