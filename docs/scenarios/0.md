# Scenario 0: Passing Tests and Working Examples

> The project SHALL always have passing tests & working examples.

## Completed

Scenario 0 rework was the broken docs command path. User-facing commands were updated from legacy name/number selectors to the current runtime-loading CLI (`module:function` or `-f <file> <function>`), and validated with real runs.

## Validation

- `gleam test` passes (`247 passed, 0 failed`).
- `gleam run -m thingfactory/cli -- run -f src/thingfactory/examples.gleam basic_pipeline --isolator local -c` succeeds.
- `gleam run -m thingfactory/cli -- run thingfactory@examples:basic_pipeline --isolator local -c` succeeds.
- `gleam run -m thingfactory/cli -- list` succeeds and shows runtime-loading usage.

## Evidence

| File | Evidence |
|---|---|
| `README.md` | Quick Start commands now use valid runtime-loading syntax and working example selectors. |
| `docs/GETTING_STARTED.md` | Example run commands switched to `module:function` and `-f` forms; removed stale embedded-pipeline list output. |
| `docs/RUNNING_PIPELINES.md` | CLI reference updated to runtime pipeline resolution and valid example function names. |
| `docs/TROUBLESHOOTING.md` | "Unknown pipeline" guidance updated to current selector requirements. |
| `docs/HOSTING_SERVICE.md` | Docker CLI example updated to runtime module selector syntax. |
